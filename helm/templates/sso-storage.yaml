kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  creationTimestamp: null
  labels:
    fdk.service: sso-claim0
  name: sso-claim0
  namespace: {{ required "Missing value NAMESPACE" .Values.NAMESPACE }}
spec: {{ if (.Values.FDK_TEMPDISK) and .Values.FDK_TEMPDISK "true" }}{{ else }}
  storageClassName: ""
  volumeName: {{ required "Missing value FDK_SSO_VOLUMENAME" .Values.FDK_SSO_VOLUMENAME }}{{ end }}
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ required "Missing value FDK_SSO_STORAGESIZE" .Values.FDK_SSO_STORAGESIZE }}
status: {}
{{ if (.Values.FDK_TEMPDISK) and .Values.FDK_TEMPDISK "true" }} {{ else }}
---
kind: PersistentVolume
apiVersion: v1
metadata:
  name: {{ required "Missing value FDK_SSO_VOLUMENAME" .Values.FDK_SSO_VOLUMENAME }}
  namespace: {{ required "Mssing value NAMESPACE" .Values.NAMESPACE }}
spec:
  storageClassName: ""
  capacity:
    storage: {{ required "Missing value FDK_SSO_STORAGESIZE" .Values.FDK_SSO_STORAGESIZE }}
  accessModes:
    - ReadWriteOnce
  gcePersistentDisk:
    pdName: {{ required "Missing value FDK_SSO_VOLUMENAME" .Values.FDK_SSO_VOLUMENAME }}
    fsType: ext4
{{ end }}